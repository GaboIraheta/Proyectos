#include <vector>
#include <list>
#include <stack>
#include <queue>
#include <hashtable.h>
#include <iostream>
#include <unordered_map>

using namespace std;

int main(void) {
    //Declaraci[on e inicializacion del vector
    vector<int> Vector = {1, 2, 3, 4, 5};
    //Acceder a los elementos del vector
    cout << "Primer elemento: " << Vector[0] << endl;
    //Añadir un elemento al final
    

    return 0;
}